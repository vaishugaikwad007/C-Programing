//string length
#include <stdio.h>
#include <string.h>
int main()
{
    char arr[]="Ashwani";
    int len1=strlen(arr);
    printf("string=%s length=%d\n",arr,len1);
    return 0;
}

OUTPUT:
string=Ashwani length=7
